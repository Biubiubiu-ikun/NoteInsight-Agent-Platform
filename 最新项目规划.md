# NoteInsight Agent Platform 最新项目规划

> 版本：V6.6，更新日期：2026-07-15
> 本文件是项目唯一权威规划。所有以“旧版”开头或带旧版本号的规划仅作历史参考；冲突内容以本文为准。

## 1. 产品目标

NoteInsight 面向内容平台创作者与运营人员，从图文笔记、OCR、评论和行为事实中提取可追溯证据，完成检索、分析和有引用的洞察报告。

核心闭环：

`账号与内容社区 -> 行为事件 -> 日事实 -> Evidence Store -> 混合检索 -> 离线评测 -> 洞察 Agent -> 可视化工作台`

## 2. 技术基线

- Backend：Go、Gin、sqlx、PostgreSQL 16，不引入 GORM。
- Cache/Ranking：Redis 7。
- Async：Transactional Outbox、NATS JetStream、幂等消费、DLQ。
- Frontend：React、TypeScript、Vite、Lucide。
- Quality：Go test/race/vet、真实 PostgreSQL/NATS 集成测试、Vitest 覆盖率门槛、Playwright E2E、Compose acceptance、OpenAPI 路由漂移、CodeQL SARIF 门禁、依赖与镜像审计。
- Observability：Prometheus、Grafana、结构化日志、request/trace correlation、告警规则。
- 代码边界：handler 只解析参数、读取鉴权上下文和响应；规则在 service，SQL 在 repository。

## 3. 当前阶段矩阵

| 阶段 | 状态 | 已交付 |
| --- | --- | --- |
| Phase 1 | 完成 | Go skeleton、配置、健康检查、migration runner |
| Phase 2B | 完成 | notes/media/comments/likes/collects/shares，移除 danmu |
| Phase 2C | 完成 | 注册登录、JWT、Refresh Session、作者/admin/banned 权限 |
| Phase 3 | 完成 | Redis 缓存、热帖热评、Prometheus、seedgen、k6 |
| Phase 4A-4D | 完成 | Outbox、JetStream、幂等 worker、DLQ、故障恢复、最终一致性 |
| Phase 5A-5B | 完成 | 行为模拟器、中文图文语料、OCR/评论、Ground Truth |
| Phase 5C | 完成 | note/user daily facts、run lineage、可重复物化 |
| Phase 6A | 完成 | 容量矩阵、冷热/热点/阶跃测试与资源证据 |
| Phase 6B | 实现完成，长时性能门槛开放 | 4.21M 行隔离库、10 分钟 soak；混合负载尾延迟仍需长期优化 |
| Phase 6C | 完成并形成 v0.6.4 基线 | 检索前一致性、安全、边界、评测、运维与测试加固 |
| Release hardening | 完成，远端 CI 已启用 | 独立冻结评测集、深度集成/E2E、契约与供应链门禁、安全依赖升级、GitHub 私有远端与版本标签 |
| Frontend Console | MVP 完成 | Feed、搜索、热榜、登录、发帖、详情、评论、互动、深链接、系统状态 |
| Phase 7A | 下一阶段 | Evidence Store 与确定性摄取 |
| Phase 7B | 未开始 | 词法/向量/混合检索与离线评测 |
| Phase 8 | 未开始 | 有引用的 Insight Agent |
| Phase 9 | 未开始 | 正式产品体验与生产化 |

## 4. Phase 6C 完成基线

### 4.1 数据与权限边界

- 已建立 `projects`、`project_members`、`datasets`、`dataset_notes`。
- `notes` 支持 `public/project` 可见性、`content_version` 和软删除。
- 每个项目自动获得 `community` 数据集，新用户自动加入默认公共项目。
- 内容创建校验项目写权限；读取按项目成员与可见性过滤。
- `evidence_sources` 已回填 note、media、comment，保存项目、数据集、版本、SHA-256、可见性、索引状态和删除状态。
- Evidence Source 触发器覆盖创建、内容版本切换、评论软删除和父笔记删除传播。

### 4.2 一致性与事件

- `note.updated`、`note.deleted` 与业务更新在同一事务写 Outbox。
- 登录用户读取详情产生 `note.viewed`；迟到的浏览事件遇到已删笔记时保留行为事实并幂等忽略派生计数。
- Event Envelope 固定 `schema_version=1`，包含 producer、correlation_id、trace_id。
- Worker 拒绝未知版本；互动增删与评论点赞继续由唯一约束和 processed event 保证幂等。
- Counter reconcile 使用循环批次检查点，排名 SQL 只返回需要的 Top-N。

### 4.3 账号与安全

- Access Token 默认 30 分钟；生产环境最长 2 小时。
- Refresh Token 一次一换且只存 hash；旧 Token 重用撤销活动会话。
- 浏览器 Refresh Token 使用 HttpOnly/SameSite Cookie，前端不写 localStorage。
- 认证入口按 IP 限流，内容写入按 user 限流。
- CORS 来源和可信代理必须显式配置；生产禁止通配来源。
- 管理员/作者修改生成结构化 audit mutation 日志。

### 4.4 运维与恢复

- migration 使用 PostgreSQL advisory lock 和 SHA-256 checksum，连续执行稳定跳过。
- 提供 maintenance、materialize、reconcile、dlqctl、backup/restore 命令。
- Prometheus/Grafana 配置覆盖 HTTP、DB pool、Outbox、JetStream、event lag 与 reconcile。
- 2026-07-15 已完成 custom-format 备份与隔离恢复演练；恢复库完成表计数校验后销毁。
- 数据删除、保留、PII、检索过滤和审计规则见 `docs/14_data_governance.md`。

### 4.5 内容与前端闭环

- 支持 like/unlike、collect/uncollect、comment like/unlike。
- Note 返回 author projection 与 viewer interaction state。
- Feed 使用服务端查询、摘要与 keyset pagination；热榜一次返回 Note 摘要，消除前端 N+1。
- 详情使用 `/notes/{id}` 深链接，分享链接可直接打开目标笔记。

### 4.6 语料与评测

- 最新质量运行 `phase6c_quality_v2_20260715`：200 notes、800 media、40000 comments、1619 eval cases。
- 单帖任务覆盖 summary、procedure、controversy、audience、ocr_detail、conflict_resolution、temporal_constraint、no_answer。
- 增加 cross_note_compare；Gold Source 精确到 note_id。
- Metadata 包含 schema_version、difficulty、answerable；质量检查全部通过。
- 独立检索基准 `retrieval_v3_20260715` 不调用原有 `contentgen.GenerateEvalCases`，固定 240 个唯一问题：development 80、holdout 160。
- semantic paraphrase、错别字、时间冲突、跨笔记比较、无答案和越权边界各 40 条；holdout 在算法开发期间禁止查看或改写。
- v3 总校验和为 `cb1494b76b38a23e0e20190614c104e1e7e22baa35bbb771cc340236335a3d35`；逐 case SHA-256、重复问题、统计与 manifest 均由 CLI/CI 重算。
- v1/v2 仅保留数据库历史审计，不用于质量声明；v2 的重复问题缺陷促成 v3。v3 已模型辅助抽样审阅并全部机器校验，尚无独立人工标注一致性结论。

### 4.7 最终验收快照（2026-07-15）

- 11 个 migration 在空库完整应用并二次幂等跳过，已应用 migration checksum 无漂移；冻结 benchmark 触发器拒绝修改。
- 当前主库包含 5,479 条活跃笔记、6,774 条媒体、101,631 条活跃评论和 113,858 条活跃 Evidence Source；未发送 Outbox 为 0。
- Go 全量测试、Linux race、vet、25% 覆盖率门槛和 6 组真实 PostgreSQL/NATS 集成测试通过；实际 Go statement coverage 为 26.13%。
- 前端 8 个测试文件/13 个测试通过，statement coverage 60.84%；typecheck、生产构建和桌面/Pixel 7 Playwright E2E 通过。
- OpenAPI 3.1 官方校验、Gin 路由双向漂移、Prometheus 配置/9 条告警、Actionlint、Gitleaks 均通过。
- scratch 镜像使用 Go 1.26.5、`x/crypto 0.52.0`、`x/net 0.55.0`、`quic-go 0.59.1`；govulncheck 可达漏洞为 0，Trivy 可修复 HIGH/CRITICAL 为 0，并生成含 171 packages 的 SPDX SBOM。
- API、Worker、PostgreSQL、Redis、NATS、Prometheus、Grafana 与前端运行正常；Prometheus targets 全部 up，JetStream pending/ack/redelivery 均为 0。
- 最终 custom-format 备份为 `noteinsight_20260715_065618.dump`，SHA-256 为 `CE2E7486F9F736EFD27A8B26485D9006A9ADB0A8C8C136DE437B0089F659439C`；隔离恢复演练已通过。
- Compose 默认使用 Gin release 模式；开发环境 CORS 白名单和 trusted proxy 显式配置。
- 私有 GitHub 远端已建立并推送 `v0.6.4`、`v0.6.5`；CI 在 Linux Runner 上执行完整质量链。CodeQL 当前生成并校验本地 SARIF 构件，开通 GitHub Code Security 后可通过仓库变量恢复平台上传。

## 5. 当前开放门槛

以下项目不阻塞 Phase 7A，但阻塞“生产就绪”声明：

1. 在增量 reconcile 与 DB pool 指标基础上完成 30 分钟 warm mixed soak，并关闭严格 P95/P99 门槛。
2. 接入真正的 OpenTelemetry exporter 与 PostgreSQL 慢查询/`pg_stat_statements`，形成跨 HTTP/SQL/Redis/NATS trace。
3. 在部署环境落地托管密钥、TLS、NATS/Redis 认证、私网隔离、镜像签名/仓库策略与 PITR。
4. 增加多实例、外部压测机和生产相似资源配额下的容量证据。
5. Git 远端、CODEOWNERS、PR 模板和安全策略已建立；GitHub API 已确认 Free 套餐不能保护私有仓库主分支，需升级 Pro 或未来经人工确认改为公开仓库。环境晋级仍需部署环境。
6. 对冻结 holdout 做独立人工抽样复核并记录标注一致性，作为公开检索质量结论的附加证据。

## 6. Phase 7A：Evidence Store 与确定性摄取

目标：把内容源与日事实转换为可版本化、可删除、可引用的 canonical evidence。

1. 定义 `EvidenceDocument`、`EvidenceChunk`、`SourceCitation`、`IngestionRun`。
2. 摄取 note title/body、media caption/OCR、高价值评论簇和 daily facts。
3. 使用 `content_hash + source_version + parser_version` 幂等摄取。
4. 保存 project、dataset、note/media/comment、字符区间、时间、可见性和删除 lineage。
5. PostgreSQL 先保存 canonical evidence 和 `tsvector`；不先引入向量库。
6. 建立增量摄取、失败重试、重建、删除传播和一致性审计命令。
7. 所有查询在评分前强制过滤 project/dataset/visibility/deleted 状态。

Phase 7A 验收：重复摄取无重复 chunk；更新只产生新版本；删除无残留活跃证据；citation 可回源到原文区间；全量重建结果确定。

## 7. Phase 7B：检索与离线评测

1. 先实现 PostgreSQL FTS/BM25 风格 lexical baseline。
2. 使用冻结的 `retrieval_v3`：development 用于调参，holdout 只用于版本化验收；先固定指标和失败分类再接 embedding/Qdrant。
3. 实现 lexical/vector/hybrid、metadata filter 和 rerank 接口。
4. 报告 Recall@K、MRR、nDCG、no-answer accuracy、citation precision、延迟和成本。
5. 每个结果必须返回可解析 citation，禁止只报告单一平均分。

建议门槛：Recall@10 >= 0.85、MRR@10 >= 0.70、No-answer accuracy >= 0.85、Citation precision >= 0.90。

## 8. Phase 8：Insight Agent

1. 单 Agent 流程：意图解析 -> 检索计划 -> 证据收集 -> 分析 -> 引用校验 -> 报告。
2. 工具调用具备超时、重试、预算、取消和 trace。
3. 输出受众画像、主题趋势、争议点、反馈、机会与风险。
4. 每个事实性结论必须关联 Evidence Source/Citation。
5. 保存 prompt、model、tool、run 和评测 lineage。

## 9. Phase 9：产品与生产化

- Agent 查询/报告 UI、证据抽屉、历史运行、评测看板。
- OpenTelemetry、SLO、容量、PITR、恢复演练、TLS、Secret 管理和网络隔离。
- 正式文件上传、内容审核、隐私删除与数据保留策略。
- 发布版本、ADR、Changelog 和 benchmark before/after。

## 10. 明确暂不做

- 不在 lexical baseline 与固定评测集之前堆叠多 Agent 或向量组件。
- 暂不做真实短信、邮箱、OAuth 与真实图片上传。
- 不为展示而提前引入 Kubernetes、Service Mesh 或微服务拆分。
